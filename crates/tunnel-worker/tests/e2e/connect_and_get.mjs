// Placeholder. The spike's end-to-end client must speak the postcard `Frame`
// protocol, so it is a native Rust binary (crates/spike-client), not Node.
// The real Node/JS client is introduced by tunnel-client in Plan 3.
import process from "node:process";
console.error("Replaced by tunnel-client in Plan 3; spike uses crates/spike-client.");
process.exit(0);
